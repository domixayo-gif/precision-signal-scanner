name: Precision Signal Scanner V2.2 Continuous

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: precision-signal-scanner-continuous
  cancel-in-progress: false

jobs:
  scan:
    name: Run V2.2 Continuous Scanner
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Python 3.12
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Validate Python files
        run: |
          python -m py_compile scanner.py
          python -m py_compile storage.py

      - name: Run continuous scanner
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          for i in $(seq 1 65)
          do
            echo "========================================"
            echo "SCAN $i OF 65"
            echo "Started: $(date -u)"
            echo "========================================"

            python scanner.py || echo "Scanner returned an error. Continuing."

            if [ "$i" -lt 65 ]; then
              echo "Waiting 5 minutes for the next scan..."
              sleep 300
            fi
          done

          echo "Continuous scanner session finished."
